package org.bottomup.arithmetic

import Arithmetic.{Bool, EndsWith, Env, Eq, ErrTy, Expr, IntTy, IsAlpha, IsDigit, Lower, Minus, Num, NumV, Plus, Str, StrReplace, StrSplit, StringTy, StringV, Times, Type, Value, Var, Variable, aryOf, canMake, eval, init, mkCode, mkCodeMultipleCtx, tyOf, verify}

import scala.collection.mutable


object Main {
  def main(args: Array[String]): Unit = {
  }
}
